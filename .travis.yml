sudo: false

language: ruby

rvm:
  - 2.3.3
  - 2.4.0

cache: bundler
script: bundle exec middleman build

# safelist
branches:
  only:
  - master

deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master
